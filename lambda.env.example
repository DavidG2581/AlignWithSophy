# Example environment variables to configure on the AWS Lambda function.
# Set these in the Lambda console or your IaC templates (they are not used by the static site).

AWS_REGION=us-east-1
ALLOWED_ORIGIN=https://www.serenityyoga.com
